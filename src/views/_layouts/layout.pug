doctype html
html(lang='fr')
    head
        meta(charset='utf-8')
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        base(href="/")
        title #{title || "MartinCtrl"}

        meta(name="description" content="Boilerfolio desc")

        link(rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96")
        link(rel="icon" type="image/svg+xml" href="/favicon.svg")
        link(rel="shortcut icon" href="/favicon.ico")
        link(rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png")
        meta(name="apple-mobile-web-app-title" content="Boilerfolio")
        link(rel="manifest" href="/site.webmanifest")


    body(style="visibility:hidden")
        include ../_includes/preloader.pug
        include ../_includes/navigation.pug
        .content(data-template=template)
            block content

    script.
        //- window.ASSETS = JSON.stringify(prismic.assets).replace(/<\//g, "<\\/");
    script(type="module", src="/src/app/index.js")